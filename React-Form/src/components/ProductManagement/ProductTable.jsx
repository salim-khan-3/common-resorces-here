import React from 'react';

const ProductTable = ({products}) => {
    return (
        <div>
            <h1>table legnth:{products.length}</h1>
        </div>
    );
};

export default ProductTable;